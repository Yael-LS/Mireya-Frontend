# Dirección del agente. En local, deja el valor predeterminado.
BACKEND_URL=http://127.0.0.1:8000

# Opcional: debe coincidir con SERVICE_API_KEY del backend. No usar NEXT_PUBLIC_:
# la ruta /api/chat lo envía desde el servidor y no se expone al navegador.
API_KEY=
